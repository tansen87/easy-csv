<!-- Generated -->
# xan eval

```txt
评估单个 moonblade 表达式.

Usage:
    xan eval [options] <expr>
    xan eval --help

eval options:
    -E, --explain          打印具体表达式计划并美化打印求值结果.
    -H, --headers <names>  伪标题,以逗号分隔.
    -R, --row <values>     伪行,以逗号分隔的单元格.

Common options:
    -h, --help  显示帮助
```
